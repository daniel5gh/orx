dependencies {
    // because we need to peek into texture internals
    compile "org.openrndr:openrndr-gl3:$openrndrVersion"
    implementation "org.lwjgl:lwjgl-openvr:$lwjglVersion"
    runtime "org.lwjgl:lwjgl-openvr:$lwjglVersion:natives-windows"
}
